PATH=$PATH:$HOME/.rvm/bin # Add RVM to PATH for scripting

if [ "`type -t rvm`" == "function" ]; then
  echo -ne "${GREEN}RVM is present => ${NORMAL} "
  rvm gemdir
else
  echo -ne "${YELLOW}rvm is not present.${NORMAL}"
fi
